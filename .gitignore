# Downloaded/extracted kernel sources and build artifacts -- all of this
# regenerates via install.sh / compile_kernel.sh, must never end up in git
# (several GB per version).
linux-*/
linux-*.tar.*
linux_*.tar.*
linux_*.diff*
linux_*.dsc
*.deb
*.buildinfo
*.changes
*_TESTCOPY
